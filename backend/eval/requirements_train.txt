# ClawTeam 训练专用依赖
# 5070 Ti 笔记本（CUDA 12.x）适配
# 安装: pip install -r requirements_train.txt

# === 深度学习核心 ===
# RTX 50 系列需要 PyTorch 2.4+ + CUDA 12.4
# 安装命令（如果默认 pip 装的版本有 CUDA 兼容问题，按下面命令重装）：
# pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0

# === HuggingFace 生态 ===
transformers>=4.45.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0          # LoRA 微调
trl>=0.11.0           # SFT trainer
bitsandbytes>=0.44.0  # 4-bit 量化（小显存救星）

# === 多模态 ===
qwen-vl-utils>=0.0.8
pillow>=10.0.0

# === 数据处理 ===
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
jieba>=0.42.1         # 中文分词

# === 实验追踪（可选，强烈推荐）===
wandb>=0.18.0
tensorboard>=2.15.0

# === Notebook 环境 ===
jupyterlab>=4.0.0
ipywidgets>=8.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# === 评测指标 ===
evaluate>=0.4.0
sacrebleu>=2.4.0
rouge-chinese>=1.0.3

# === LLM API（用于自动标注数据）===
openai>=1.50.0        # DeepSeek/Qwen 兼容此 SDK
